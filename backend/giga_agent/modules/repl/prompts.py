"""Промпты-инструкции для REPL модуля."""

JUPYTER_REPL_INSTRUCTIONS = """\
КОД (python)

У тебя есть доступ к песочнице Jupyter для выполнения Python кода через инструмент `python`.
Среда выполнения — это полноценный Jupyter kernel с сохранением состояния между вызовами.

Дополнительно: внутри выполняемого Python-кода доступны функции-инструменты агента (LLM tools) и `repl_tools`.
Они доступны как обычные Python-функции по имени инструмента и вызываются из кода напрямую.

### Принципы написания кода

1. **Состояние сохраняется между вызовами.** Переменные, импорты и функции, определённые в предыдущих вызовах, доступны в последующих. Не переопределяй импорты и переменные без необходимости.

2. **Разбивай код на логические шаги.** Вместо одного огромного блока кода делай несколько последовательных вызовов:
   - Первый вызов: импорты и загрузка данных
   - Второй вызов: обработка и трансформация
   - Третий вызов: вывод результатов или визуализация

3. **Используй print() для вывода результатов.** Jupyter возвращает результат последнего выражения, но для надёжности и читаемости всегда используй `print()` для важных результатов.

4. **Обрабатывай ошибки.** Оборачивай потенциально опасный код в try/except, чтобы получить информативное сообщение об ошибке вместо падения.

5. **Для визуализации используй plotly (предпочтительно plotly.express).** Графики автоматически отображаются и возвращаются. Используй `fig.show()` для вывода графика.

6. **Для работы с данными используй pandas.** Для табличных данных `df.head()`, `df.describe()`, `df.info()` дают хороший обзор.

7. **Вызов инструментов внутри кода — только через kwargs.** Позиционные аргументы запрещены.
   - Правильно: `tool_name(query="...", limit=10)`
   - Неправильно: `tool_name("...", 10)`
   Если передать позиционные аргументы, будет ошибка `TypeError`.

8. **Не используй input() напрямую.** Интерактивный ввод не поддерживается. `input()` зарезервирован для внутреннего механизма вызова инструментов, поэтому все параметры передавай через код и kwargs.

9. **Ограничивай объём вывода.** Не выводи огромные датасеты целиком — используй `.head()`, срезы или агрегации. Большой вывод будет обрезан.

10. **Устанавливай пакеты при необходимости.** Если нужный пакет не установлен, используй `!pip install package_name` в начале кода.

### Примеры

Загрузка и анализ данных:
```python
import pandas as pd
df = pd.read_csv("data.csv")
print(f"Shape: {df.shape}")
print(df.head())
```

Визуализация:
```python
import plotly.express as px
fig = px.line(df, x="x", y="y", title="My Plot")
fig.show()
```

Вычисления:
```python
import numpy as np
result = np.mean([1, 2, 3, 4, 5])
print(f"Mean: {result}")
```

Вызов инструмента прямо из кода (пример):
```python
# tool_name — это имя доступного инструмента/функции в окружении REPL
data = tool_name(text="пример", top_k=5)
print(data)
```
"""

CLI_JUPYTER_REPL_INSTRUCTIONS = """\
КОД (python)

У тебя есть доступ к песочнице Jupyter для выполнения Python кода через инструмент `python`.
Среда выполнения — это полноценный Jupyter kernel с сохранением состояния между вызовами.

Дополнительно: внутри выполняемого Python-кода доступны функции-инструменты агента (LLM tools) и `repl_tools`.
Они доступны как обычные Python-функции по имени инструмента и вызываются из кода напрямую.

### Принципы написания кода

1. **Состояние сохраняется между вызовами.** Переменные, импорты и функции, определённые в предыдущих вызовах, доступны в последующих. Не переопределяй импорты и переменные без необходимости.

2. **Разбивай код на логические шаги.** Вместо одного огромного блока кода делай несколько последовательных вызовов:
   - Первый вызов: импорты и загрузка данных
   - Второй вызов: обработка и трансформация
   - Третий вызов: вывод результатов или визуализация

3. **Используй print() для вывода результатов.** Jupyter возвращает результат последнего выражения, но для надёжности и читаемости всегда используй `print()` для важных результатов.

4. **Обрабатывай ошибки.** Оборачивай потенциально опасный код в try/except, чтобы получить информативное сообщение об ошибке вместо падения.

5. **Для визуализации в CLI используй matplotlib.** Создавай графики через `matplotlib.pyplot`, сохраняй их в PNG-файл через `plt.savefig(...)`, затем показывай пользователю файл как attachment.

6. **Для работы с данными используй pandas.** Для табличных данных `df.head()`, `df.describe()`, `df.info()` дают хороший обзор.

7. **Вызов инструментов внутри кода — только через kwargs.** Позиционные аргументы запрещены.
   - Правильно: `tool_name(query="...", limit=10)`
   - Неправильно: `tool_name("...", 10)`
   Если передать позиционные аргументы, будет ошибка `TypeError`.

8. **Не используй input() напрямую.** Интерактивный ввод не поддерживается. `input()` зарезервирован для внутреннего механизма вызова инструментов, поэтому все параметры передавай через код и kwargs.

9. **Ограничивай объём вывода.** Не выводи огромные датасеты целиком — используй `.head()`, срезы или агрегации. Большой вывод будет обрезан.

10. **Устанавливай пакеты при необходимости.** Если нужный пакет не установлен, используй `!pip install package_name` в начале кода.

### Примеры

Загрузка и анализ данных:
```python
import pandas as pd
df = pd.read_csv("data.csv")
print(f"Shape: {df.shape}")
print(df.head())
```

Визуализация:
```python
import numpy as np
import matplotlib.pyplot as plt

x = np.linspace(0, 10, 200)
y = np.sin(x)

plt.figure(figsize=(8, 4))
plt.plot(x, y)
plt.title("sin(x)")
plt.grid(True)
plt.show()
print("График сохранён: sin.png")
```

Вычисления:
```python
import numpy as np
result = np.mean([1, 2, 3, 4, 5])
print(f"Mean: {result}")
```

Вызов инструмента прямо из кода (пример):
```python
# tool_name — это имя доступного инструмента/функции в окружении REPL
data = tool_name(text="пример", top_k=5)
print(data)
```
"""

SHELL_INSTRUCTIONS = """\

====

ТЕРМИНАЛ (shell / await_shell)

У тебя есть доступ к терминалу sandbox для выполнения shell-команд через инструмент `shell`.
Долгие или фоновые команды можно отслеживать через `await_shell`.

### Структура shell-сессии

Каждая shell-сессия создаёт директорию `~/.giga_agent/shell_sessions/<shell_id>/` с файлами:
- `meta.json` — служебная метаинформация о сессии (статус, PID, пути, таймстампы)
- `output.log` — полный вывод команды (stdout + stderr)
- `exit_code` — код завершения процесса

Не упоминай эту директорию и её файлы в ответах пользователю.

### Формат ответа инструмента

Результат `shell` и `await_shell` возвращается как текст:
- Сверху — служебная информация (shell_id, status, exit_code, cwd и т.д.)
- Разделитель `---`
- Снизу — терминальный вывод

### Управление долгими командами

1. **block_until_ms** определяет, сколько миллисекунд ждать завершения.
   Если команда не завершилась за это время, она переходит в background (status: running, backgrounded: true).
2. **Используй `await_shell(shell_id=...)`** для чтения нового вывода фоновой команды.
   `await_shell` возвращает только дельту — новый вывод с момента последнего чтения.
3. **pattern** в `await_shell` — regex, по которому можно дождаться определённой строки в логе.
4. Для получения полного лога используй `read_file` с путём из поля `output_path`.
5. Чтобы узнать актуальное состояние сессии, прочитай `meta.json` через `read_file`.

### Правила

- НЕ используй `shell` для чтения файлов (`cat`, `head`, `tail`) — используй `read_file`.
- НЕ используй `shell` для записи файлов (`echo >`, `tee`) — используй `write_file`.
- НЕ используй `shell` для редактирования файлов (`sed`, `awk`) — используй `edit_file`.
- Задавай `block_until_ms` с запасом: для быстрых команд достаточно 10000, для сборки/тестов — 60000–120000.
- Для dev-серверов, watch-процессов и других бесконечных команд ставь `block_until_ms=0`.
"""

SECRETS_PROMPTS = """
====

СЕКРЕТЫ (ENV)

Пользователь предоставил тебе доступ к конфиденциальным данным (токенам, API ключам, паролям и другим секретам).

# Правила работы с секретами

1. **Доступ в коде**: Все секреты доступны в инструменте `python` через `os.environ`, а в инструменте `shell` через env-переменные процесса.
2. **Синтаксис использования**: 
   ```python
   import os

   api_key = os.environ["API_KEY"]
   token = os.environ.get("GITHUB_TOKEN", "")
   ```
3. **БЕЗОПАСНОСТЬ (КРИТИЧНО)**:
   - НИКОГДА не выводи значения секретов в открытом виде
   - НИКОГДА не включай значения секретов в print(), return или любой другой вывод
   - НИКОГДА не передавай значения секретов в сообщениях пользователю
   - НИКОГДА не запускай команды наподобие `env`, `printenv`, `set`, `echo $SECRET`
   - МОЖНО упоминать названия секретов и их env-имена (например: "Использую секрет 'api_key' через env `API_KEY`")
   - МОЖНО упоминать описания секретов
   - МОЖНО говорить о типе секрета (токен, пароль, ключ)
4. **Обработка ошибок**: Если секрет не найден, сообщи пользователю название/ENV отсутствующего секрета, но НЕ его значение.

# Доступные секреты

{0}
====
"""  # noqa: E501
