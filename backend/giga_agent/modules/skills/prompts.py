"""Промпты для Skills модуля."""

SKILLS_SYSTEM_PROMPT_HEADER = """\
## Доступные навыки (Skills, скиллы)

У тебя есть доступ к следующим навыкам. Работа с навыком — двухшаговая:
1. Вызови `read_skill_manifest(name="<skill_name>")`, чтобы прочитать его \
манифест (SKILL.md) и получить список вспомогательных файлов.
2. По мере необходимости читай конкретные файлы навыка из списка через \
`read_file` (там лежат детальные инструкции по фичам). Манифест — НЕ полная \
документация, в нём намеренно даны только заголовки и ссылки.

ВАЖНО: указывай имя навыка ТОЧНО как написано в списке ниже, включая любые \
префиксы (например `agent/...`). Слэш — часть идентификатора, а не путь. \
Сокращения и упрощения имени недопустимы.

"""

SKILLS_EXPLICIT_ACTIVATION_HINT = (
    "Пользователь явно запросил навык '{name}'. "
    "Вызови read_skill_manifest(name=\"{name}\"), затем при необходимости "
    "читай файлы навыка через read_file."
)
